import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Chimney Services",
  description:
    "Professional chimney inspections, cleanings, repairs, rebuilds, liners, crowns, and fireplace insert installations in Madison, WI.",
};

export default function ChimneyServicesPage() {
  return <main />;
}
